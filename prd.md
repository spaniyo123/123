# PRD: 부서 공용 할일 관리 앱

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-06-18 |
| 대상 조직 | 부서 내 팀원 전체 |
| 환경 전제 | 내부 신뢰 환경 (외부 비공개) |

---

## 1. 개요

부서원 전체가 공용으로 사용하는 웹 기반 할일 관리 앱이다. 로그인 없이 이름만 입력하면 즉시 사용 가능하며, 칸반보드·리스트·캘린더·대시보드 4가지 뷰로 동일한 데이터를 다양한 관점에서 확인할 수 있다. 모든 데이터는 Supabase(Postgres) 기반 공용 저장소에 저장되어 팀원 간 실시간으로 공유된다.

## 2. 목적

- 부서 내 흩어진 업무(메신저, 엑셀, 구두 전달)를 단일 화면에서 등록·추적
- 담당자별 업무 부하와 마감 임박/지연 현황을 시각적으로 즉시 파악
- 로그인 절차 없이 누구나 빠르게 진입해 사용 장벽을 최소화
- 실시간 동기화로 "누가 뭘 하고 있는지" 항상 최신 상태로 공유

## 3. 사용자 시나리오

1. **신규 진입**: 팀원이 링크로 접속 → 이름 입력 → 칸반보드 진입, 별도 가입 절차 없음
2. **할일 등록**: 팀원이 "분기 보고서 작성" 할일을 등록하고 담당자, 우선순위(high), 마감일(3일 후)을 지정
3. **상태 변경 및 협업**: 담당자가 칸반보드에서 카드를 "진행중"으로 드래그하고, 이슈 발생 시 댓글로 팀원에게 공유
4. **마감 관리**: 팀원이 캘린더 뷰에서 이번 주 마감 항목을 확인하고, 마감 3일 이내 항목은 긴급 배지로 표시되어 우선 처리
5. **현황 파악**: 팀장이 대시보드에서 담당자별 미완료 건수, 전체 완료율, 지연 건수를 확인하고 업무 재배분 결정
6. **실시간 반영**: A가 카드를 수정하는 동안 B의 화면에도 변경 사항이 별도 새로고침 없이 자동 반영됨

## 4. 기능 요구사항

### 4.1 할일(Task) CRUD 및 상태 관리
- 등록·수정·삭제, 상태 전환: `todo → in-progress → done` (역방향 전환도 허용)
- 담당자 지정(자유 텍스트), 우선순위(`high`/`medium`/`low`), 마감일 설정
- 카테고리 분류(선택)

### 4.2 댓글 기능
- 할일별 댓글 스레드 (작성자, 내용, 작성 시각 표시)
- 댓글 수정/삭제는 1차 범위에서 제외 (Out of Scope 참조)

### 4.3 검색 및 필터
- 검색: 제목, 담당자명 텍스트 검색
- 필터: 상태 / 우선순위 / 담당자 / 카테고리 (다중 선택 가능)
- **4개 뷰(칸반/리스트/캘린더/대시보드) 전체에 동일한 검색·필터 상태가 일관 적용**됨 (뷰 전환 시 조건 유지)

### 4.4 마감 임박/지연 표시
| 조건 | 배지 |
|---|---|
| 미완료 + 마감일까지 0~3일 남음 | 🟡 "마감 임박" |
| 미완료 + 마감일 초과 | 🔴 "지연" |
| 완료(done) 상태 | 배지 미표시 |

### 4.5 통계 및 대시보드 지표
| 지표 | 정의 |
|---|---|
| 상태별 카운트 | todo / in-progress / done 각 건수 |
| 완료율 | done 건수 ÷ 전체 건수 × 100 |
| 담당자별 부하 | 담당자별 미완료(todo+in-progress) 건수 |
| 마감 임박/지연 건수 | 4.4 기준 배지가 표시되는 항목 수 집계 |

### 4.6 뷰 구성
| 뷰 | 설명 |
|---|---|
| 칸반보드 | 상태별 컬럼, 드래그앤드롭으로 상태 변경 |
| 리스트 | 표 형태, 컬럼 정렬, 일괄 필터 |
| 캘린더(월간) | 마감일 기준 항목 표시, 날짜 클릭 시 해당일 항목 목록 |
| 대시보드 | 4.5의 통계 지표 시각화(차트/카드) |

### 4.7 데이터 필드 정의

**Task (할일)**
| 필드명 | 타입 | 필수여부 | 제약 |
|---|---|---|---|
| id | UUID | 필수 | PK, 자동 생성 |
| title | string | 필수 | 1~100자 |
| description | text | 선택 | 최대 2,000자 |
| status | enum(`todo`,`in-progress`,`done`) | 필수 | 기본값 `todo` |
| assignee | string | 필수 | 1~30자 (자유 텍스트, 이름) |
| priority | enum(`high`,`medium`,`low`) | 필수 | 기본값 `medium` |
| dueDate | date | 선택 | ISO 8601 |
| category | string | 선택 | 사전 정의 목록 또는 자유 입력 |
| createdBy | string | 필수 | 1~30자, 등록 당시 입력 이름 |
| createdAt | timestamp | 필수 | 서버 자동 생성 |
| updatedAt | timestamp | 필수 | 변경마다 자동 갱신, 충돌 처리 기준값 |

**Comment (댓글)**
| 필드명 | 타입 | 필수여부 | 제약 |
|---|---|---|---|
| id | UUID | 필수 | PK |
| taskId | UUID | 필수 | FK → Task.id |
| author | string | 필수 | 1~30자 |
| content | text | 필수 | 1~1,000자 |
| createdAt | timestamp | 필수 | 서버 자동 생성 |

### 4.8 작성자 식별 정책 (로그인 없음)
- 최초 진입 시 이름을 입력해 `assignee`/`createdBy`/`author` 값으로 사용
- **한계 명시**: 별도 인증이 없으므로 동명이인 구분 불가, 타인 이름으로 입력하는 사칭을 기술적으로 막을 수 없음
- 이 한계는 **부서 내부 신뢰 환경**(외부 비공개, 구성원이 서로 식별 가능한 소규모 집단)을 전제로 허용 가능한 리스크로 간주
- 향후 개선 옵션(1차 범위 제외): 브라우저 내 이름 기억 기능, 간단한 사번 매칭 등

### 4.9 실시간 공유 및 충돌 처리 정책
- **실시간 반영**: Supabase Realtime(Postgres Changes 구독)을 통해 다른 팀원의 생성/수정/삭제가 별도 새로고침 없이 자동으로 화면에 반영 (목표 지연 2초 이내)
- **동시 편집 충돌 처리**: `updatedAt` 기준 **Last-Write-Wins** 적용
  - 클라이언트는 화면에 표시된 레코드의 `updatedAt`을 저장 요청 시 함께 전달
  - 저장 시점에 서버의 `updatedAt`이 클라이언트가 들고 있던 값과 다르면(=그 사이 다른 사용자가 수정함) "다른 팀원이 방금 수정했습니다" 안내를 표시하되, 최종 저장은 **가장 마지막에 도착한 요청을 그대로 반영**
  - 데이터 영구 손실 가능성은 사용자에게 안내하며, 이는 1차 범위에서 허용하는 단순화된 정책임

## 5. 비기능 요구사항

### 5.1 반응형 브레이크포인트
| 구분 | 너비 |
|---|---|
| Mobile | 0 ~ 639px |
| Tablet | 640 ~ 1023px |
| Desktop | 1024px 이상 |

### 5.2 성능
| 항목 | 목표 |
|---|---|
| 최초 콘텐츠 표시(FCP) | 1.5초 이내 |
| 뷰 전환(칸반↔리스트↔캘린더↔대시보드) | 300ms 이내 |
| 칸반 드래그 반응 | 100ms 이내 체감 반응 |
| 100~500건 데이터 리스트 렌더링 | 1초 이내 |

### 5.3 접근성
- 모든 인터랙션 요소(버튼, 카드, 드래그 영역) 키보드(Tab/Shift+Tab/Enter/Space)로 조작 가능
- 텍스트 명도 대비 WCAG AA 기준 준수 (일반 텍스트 4.5:1 이상, 큰 텍스트 3:1 이상)
- 모든 입력 폼에 명시적 라벨(label/aria-label) 적용
- 포커스 상태 시각적으로 명확히 표시 (focus ring)

### 5.4 보안
- 공개 anon key 사용을 전제로 **Supabase Row Level Security(RLS)** 활성화
  - 부서 공용 테이블 특성상 전 행에 대한 read/insert/update는 허용하되, delete는 별도 정책으로 제한 검토
- 사용자 입력값(제목/설명/댓글)은 저장·렌더링 시 HTML 이스케이프 처리하여 XSS 방지 (`dangerouslySetInnerHTML` 미사용)

### 5.5 지원 브라우저
- Chrome, Edge, Safari, Firefox 최신 2개 메이저 버전 (모바일 Safari/Chrome 포함)
- Internet Explorer 미지원

## 6. UI 가이드라인

- **톤앤매너**: 젊고 세련된 감각, 공공·사내용 툴 특유의 딱딱함 배제
- **컬러**: 바이올렛(#7C3AED 계열) → 인디고(#4F46E5 계열) 그라디언트를 주요 포인트(헤더, CTA 버튼, 활성 탭)에 사용
- **형태**: 카드형 UI, 둥근 모서리(border-radius 12~16px), 부드러운 카드 섀도우
- **모션**: 상태 전환·모달 오픈/클로즈 시 부드러운 트랜지션(150~250ms ease-out)
- **디자인 토큰**: 색상·spacing·radius·shadow는 모두 토큰(CSS 변수 또는 Tailwind config)으로 관리, 컴포넌트 내 하드코딩 금지
- **배지 컬러 규칙** (상태/우선순위 전역 일관 적용)

| 구분 | 값 | 컬러 |
|---|---|---|
| 상태 | todo | 그레이 계열 |
| 상태 | in-progress | 블루 계열 |
| 상태 | done | 그린 계열 |
| 우선순위 | high | 레드 계열 |
| 우선순위 | medium | 옐로/앰버 계열 |
| 우선순위 | low | 그레이/블루 계열 |
| 마감 | 임박 | 옐로 |
| 마감 | 지연 | 레드 |

## 7. 화면 구조 (IA)

```
진입 (이름 입력)
└── 메인 (검색/필터 바 - 4개 뷰 공통)
    ├── 칸반보드
    │   └── 할일 카드 → 상세 모달 (수정/삭제/댓글)
    ├── 리스트
    │   └── 행 클릭 → 상세 모달 (칸반과 동일 모달 재사용)
    ├── 캘린더(월간)
    │   └── 날짜 클릭 → 해당일 항목 목록 → 상세 모달
    └── 대시보드
        ├── 상태별 카운트 카드
        ├── 완료율 차트
        ├── 담당자별 부하 차트
        └── 마감 임박/지연 리스트
```

## 8. 개발 범위

### In Scope (1차 출시)
- 할일 등록/수정/삭제, 상태 변경
- 담당자/우선순위/마감일 설정, 카테고리
- 댓글 작성(수정·삭제 제외)
- 검색·필터 (4개 뷰 공통 적용)
- 마감 임박/지연 배지
- 통계 대시보드(4.5 지표)
- 칸반/리스트/캘린더/대시보드 4개 뷰
- 이름 기반 식별 (로그인 없음)
- Supabase 기반 실시간 동기화 및 Last-Write-Wins 충돌 처리
- 반응형 웹 (모바일/태블릿/데스크탑)

### Out of Scope (1차 제외, 향후 검토)
- 로그인/인증 시스템 (사번 연동 등)
- 댓글 수정/삭제, 댓글 대댓글
- 파일 첨부
- 알림 기능 (이메일/푸시/슬랙 연동)
- 역할 기반 권한 분리(관리자/일반 사용자)
- 다국어 지원
- 모바일 네이티브 앱, 오프라인 모드
- 타 부서로의 확장 적용

## 9. 성공 지표

| 지표 | 목표치 |
|---|---|
| 출시 2주 내 부서원 사용률 | 90% 이상 1회 이상 사용 |
| 주간 활성 사용자 비율(WAU/전체 인원) | 70% 이상 |
| 평균 할일 처리 소요일 | 도입 전 대비 20% 단축 |
| 월평균 마감 지연 건수 | 도입 전 대비 30% 감소 |
| 페이지 로드 시간(95th percentile) | 2초 이내 |
| 실시간 반영 지연 | 2초 이내 |
| 동시 편집 충돌로 인한 사용자 불만/이슈 신고 | 월 1건 이하 |
